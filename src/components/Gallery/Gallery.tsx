import type { FC } from "react";

const Gallery: FC = () => {
    return (
        <div
            className="w-full h-[50rem] border border-red-400"
        >
            <p>Section</p>
        </div>
    );
};

export default Gallery;
